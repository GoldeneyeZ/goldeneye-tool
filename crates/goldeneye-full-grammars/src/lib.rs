//! Opt-in native Tree-sitter grammar registry.
//!
//! GFP-2 deliberately leaves the generated registry disconnected from the
//! default build. GFP-3 adds the compiled feature and its audited FFI boundary.

#![deny(unsafe_code)]
